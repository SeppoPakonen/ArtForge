# Empty And Disabled State Polish

Task: `plan/05-ui-shells/036-visual-polish-pass-for-empty-and-disabled-states.md`

Polish applied:

- No-file startup now says `No file loaded` and points to Open or compatible
  Recent/Example rows.
- Start page rows use intentional action wording instead of launch-only text.
- Empty compatible recent/example state is explicit.
- No-selection inspector text reads `No item selected`.
- Provider placeholder text explains when provider rows become available.
- Visual smoke notes now cover scope-specific tabs, severity diagnostics, and
  command-state disabled behavior.

No new UI framework, icon dependency, or full editor UI was introduced.
